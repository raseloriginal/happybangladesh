name: Deploy to Draft/Staging Server

on:
  push:
    branches: [ "draft" ]

jobs:
  deploy-draft:
    runs-on: ubuntu-latest

    steps:
    - name: Deploy via SSH Git Pull
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.DRAFT_SERVER_HOST }}
        username: ${{ secrets.DRAFT_SERVER_USERNAME }}
        key: ${{ secrets.DRAFT_SSH_PRIVATE_KEY }}
        port: 22
        script: |
          cd /home/happydraft/htdocs/happydraft.raseloriginal.digital
          
          # Trust the directory for git commands
          git config --global --add safe.directory /home/happydraft/htdocs/happydraft.raseloriginal.digital
          
          # Fetch changes and reset local branch to match the remote draft branch
          git fetch origin draft
          git reset --hard origin/draft
          
          # Ensure correct ownership and permissions
          chown -R happydraft:happydraft /home/happydraft/htdocs/happydraft.raseloriginal.digital 2>/dev/null || true
          
          # Create and set permissions for logs/storage if needed
          mkdir -p storage/logs storage/framework/views storage/cache logs
          chmod -R 775 storage logs 2>/dev/null || true
          chmod -R 755 public 2>/dev/null || true
          
          echo "Draft Deploy OK at $(date)"

